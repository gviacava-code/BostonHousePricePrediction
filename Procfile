# Define a process type named web
web:
  # Run the command gunicorn app:app
  # This command tells Gunicorn to run the app object from the app module
  # By default, Gunicorn will start one worker process and bind to localhost on port 8000
  gunicorn app:app
  